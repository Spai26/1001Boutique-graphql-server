{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "node_modules", "build", "coverage"],
  "exec": "ts-node -r tsconfig-paths/register ./src/index.ts",
  "restarable": "rs",
  "env": {
    "NODE_ENV": "development | test"
  }
}
